@page "/"
@using PFM.Website.Services
@using PFM.Website.Models
@inject BankAccountService BankAccountService

<AuthorizeView>
    <Authorized>

        <div class="row">
            <div class="col-lg-8">
                <h1>Dashboard</h1>
            </div>
            <div class="col-lg-4">
                <select class="form-control" value="@SelectedBankAccount?.Id" @onchange="@((e) => UpdateSelected(e))">
                    <option value=""></option>
                    @foreach (var bankAccount in @AvailableBankAccounts)
                    {
                        <option value="@bankAccount.Id">@bankAccount.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox">
                    <div class="ibox-content">
                        @*<label style="font-size:18px;">Accounts</label>
                            <div class="row">
                                <div class="col-lg-12">
                                    <select id="availableAccounts"></select>
                                </div>
                            </div>*@
                        @*<label style="font-size:18px;margin-top: 8px;">Movements</label>
                            <div class="row">
                                <div class="col-lg-12">
                                    <a href="/Expenditure/Index">Manage expenses</a>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-lg-12">
                                    <a href="/AtmWithdraw/Index">Manage ATM Withdraws</a>
                                </div>
                            </div>*@
                        <div class="row">
                            <div class="col-lg-12">
                                <a href="/incomes/list/@SelectedBankAccount?.Id">Manage incomes</a>
                            </div>
                        </div>
                        @*<div class="row">
                                <div class="col-lg-12">
                                    <a href="/Saving/Index">Manage savings</a>
                                </div>
                            </div>*@
                    </div>
                </div>
            </div>
        </div>
    </Authorized>
</AuthorizeView>


@code {

    private BankAccountListModel? SelectedBankAccount;
    private IList<BankAccountListModel> AvailableBankAccounts = new List<BankAccountListModel>();

    protected override async Task OnInitializedAsync()
    {
        SelectedBankAccount = await BankAccountService.GetCurrentAccount();
        AvailableBankAccounts = await BankAccountService.GetAll();
    }

    protected async Task UpdateSelected(ChangeEventArgs referenced)
    {
        var id = Convert.ToInt32(referenced.Value);
        SelectedBankAccount = await BankAccountService.GetCurrentAccount(id);
    }
}
